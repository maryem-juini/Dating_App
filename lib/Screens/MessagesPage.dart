import 'package:flutter/material.dart';

class MessgesScreen extends StatelessWidget {
  const MessgesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}